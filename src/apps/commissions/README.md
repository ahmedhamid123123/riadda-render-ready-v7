Commissions app structure
=========================

- `views/` package: place UI views here (if needed).
- `api/` package: API endpoints (if any) belong here.

Guidelines:
- Create submodules under `views/` for logical separation when the app grows.
